export class Product {

    // constructing the template
    constructor (
        public imageUrl: string,
        public name: string,
        public price: number,
        public description: string
    ) { }
}